var timesheet = (function(){
  // instance variables
  var width, height;
  // internal functions

  // external functions
  return{
    display: function(cont){
      container = document.getElementById(cont);
      width = container.offsetWidth;
      height = ;

      console.log(width);
      console.log(height);
    }
  }
})();

timesheet.display("container");
